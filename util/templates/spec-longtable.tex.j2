~{# This template renders to a LaTeX table spanning a sequence of reqs/specs.

    It expects a model like:

    { code: "nd-cap", specs: [...] }

    It might be exercised like:

    moo -A code=nd-cap -A specs=data/nd-cap.jsonnet \
      render util/code-specs-model.jsonnet \
             util/templates/spec-longtable.tex.j2

#}~
% N.B. This file is generated, any edits may be lost.
\begin{footnotesize}
\begin{longtable}{p{0.12\textwidth}p{0.18\textwidth}p{0.17\textwidth}p{0.25\textwidth}p{0.16\textwidth}}
\caption{Specifications for ~{{ model.code|upper }}~ \fixmehl{ref \texttt{tab:spec:~{{ model.code }}~}}} \\
  \rowcolor{dunesky}
       Label & Description  & Specification \newline (Goal) & Rationale & Validation \\  \colhline

~{ for req in model.specs -}~
  \newtag{~{{ req.gid }}~}{ spec:~{{ req.label }}~ }  & ~{{ req.name }}~  &  ~{{ req.specification }}~ \newline (~{{ req.goal }}~) &  ~{{ req.rationale }}~ &  ~{{ req.validation }}~ \\ \colhline
~{ endfor }~

\label{tab:specs:~{{ model.code|lower }}~}
\end{longtable}
\end{footnotesize}

